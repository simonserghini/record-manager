// Pulls in the ambient `declare module "cloudflare:test"` types shipped with
// the pool package, so test files typecheck under the project tsconfig.
/// <reference types="@cloudflare/vitest-pool-workers/types" />
